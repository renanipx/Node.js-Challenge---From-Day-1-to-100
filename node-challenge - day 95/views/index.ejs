<!DOCTYPE html>
<html>
<head>
  <title>SimpleCMS - Página Inicial</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>📚 Lista de Posts</h1>

  <% if (posts.length === 0) { %>
    <p>Nenhum post encontrado.</p>
  <% } else { %>
    <% posts.forEach(post => { %>
      <div class="post">
        <h2><%= post.title %></h2>
        <p class="date">Publicado em <%= post.createdAt.toLocaleDateString() %></p>
        <p><%= post.body %></p>
      </div>
    <% }) %>
  <% } %>
</body>
</html>
